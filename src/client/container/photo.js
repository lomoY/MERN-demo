import React from "react"
// import { browserHistory } from 'react-router'

export default class Photo extends React.Component {
//   componentDidMount() {
//     browserHistory.push('/');
//   }
  render() {
    return (
      <div>
      <h2>Photo</h2>
      </div>
    );
  }
}

// 应该提供一个服务来获取照片，照片应当存在云服务器上&或者直接拿外部链接
// 用一个json文件来保存，然后向对应的json文件进行请求
//需要控制图片的大小
// 可以标注more photos can been found in....
//还有啥。。。。靠。。。